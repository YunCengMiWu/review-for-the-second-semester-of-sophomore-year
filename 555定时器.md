这个转换其实非常直接。虽然 Multisim 里的 555 定时器和 Proteus 里的 `NE555` 形状看起来不太一样，但**引脚功能和编号是完全一一对应的**。

你可以直接根据**引脚编号（1到8）**或者**引脚名称**来进行连线。下面是两个软件中 555 芯片引脚的详细对应关系对照表：

### 555 定时器引脚对应表

| 引脚编号 | 引脚功能名称 | Proteus 中的标注 | Multisim 中的标注与功能说明 |
| --- | --- | --- | --- |
| **1** | 地 (Ground) | **GND** | 对应 Multisim 底部的接地端。 |
| **2** | 触发输入 (Trigger) | **TR** | 对应 Multisim 产生时钟的关键输入端（低电平触发）。 |
| **3** | 输出 (Output) | **Q** | 对应 Multisim 中输出方波时钟信号的引脚（通常接后续计数电路）。 |
| **4** | 复位 (Reset) | **R** (带小圆圈) | 低电平复位端。Multisim 中通常直接接 5V (VDD) 使其无效。 |
| **5** | 控制电压 (Control Voltage) | **CV** | 控制端。Multisim 中通常接一个 0.01µF 的消噪电容到地。 |
| **6** | 阈值输入 (Threshold) | **TH** | 阈值比较端。通常与 2 脚 (TR) 连在一起接电容。 |
| **7** | 放电端 (Discharge) | **DC** | 放电端。在 Multisim 中通常接在两个 47kΩ 电阻中间。 |
| **8** | 电源 (Power) | **VCC** | 对应 Multisim 上方的电源端，接 5V。 |

---

### 💡 照着原图连线的快捷指南

看你上一张 Multisim 的图，左下角的 555 电路非常经典，你在 Proteus 里可以这样快速完成连线：

1. **电源与地**：将 Proteus 的 **8脚 (VCC)** 接 5V，**1脚 (GND)** 接地。
2. **复位端**：将 **4脚 (R)** 也直接接到 5V 电源上（因为原图中它接了高电平）。
3. **控制端**：将 **5脚 (CV)** 像原图一样，接一个 `0.01µF`（或 `10nF`）的电容到地。
4. **RC振荡部分**：
* 拿两个 `47kΩ` 电阻串联。
* 电阻 A 的一端接 5V，另一端接 **7脚 (DC)**。
* 电阻 B 的一端接 **7脚 (DC)**，另一端接 **6脚 (TH)**。
* 把 **6脚 (TH)** 和 **2脚 (TR)** 用导线直接连在一起。
* 从 **2脚/6脚** 的交点处，接一个 `1µF` 的电容到地。


5. **输出时钟**：**3脚 (Q)** 就是你的时钟脉冲输出端，把它引出去连接到后续的控制触发芯片（如 74LS74 或 74HC160）上即可。